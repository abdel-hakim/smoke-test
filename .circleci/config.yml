version: 2.1
jobs:
  smoke_test:
    docker:
      - image: alpine:latest
    steps:
      - run:  apk add --update curl
      - run:  
          name: smoke test script
          cmmand: |
            URL="https://blog.udacity.com/"
            if curl -s --head ${URL}
            then
              retern 0
            else
              retern 1
            fi
workflows:
  myworkflow:
    jobs:
      - smoke_test


# workflows:
#   myworkflow:
#     jobs:
#       - smoke_test:
#           requires:
#             - configure_infrastructure
